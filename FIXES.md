# core_v4 FIXES

(one entry per change: date, symptom, root cause, files, patch path, test)

- 2026-09-19 22:31  Clock-skew fix (sleep 1.5 s in _core_v4_finish, orchestration/rules/pipeline/core_v4/enrichment.smk:468): already present in the checkout when the monitoring agent started; nothing applied by the agent.
- 2026-09-19 23:18  Fix #1: SideOutput.begin() race between parallel shards. Symptom: minorities project shard 5/8 crashed, FileNotFoundError at side_outputs.py:210 `tmp.unlink()` (path .../minorities/seen/project/part-004-000096.parquet.tmp, another shard's tmp). Root cause: begin() removed ALL `*.tmp` in the shared side-output dir, i.e. also the live tmp files of concurrently running shards (crash on rename race, or a live shard's os.replace failing). Files: src/pipelines/core_v4/enrichment/side_outputs.py (`_TMP_RE`, `_own_tmps()`, begin() removes only own (tier, shard) tmps, missing_ok), test_side_outputs.py (`test_begin_only_removes_own_shard_tmp`). Patch: fixes/01-side-output-tmp-race.patch. Test: test_side_outputs.py 17 passed (re-run by monitoring agent). Rerun: same Snakemake command resumes the failed shard. Noted, NOT fixed: topic_modelling.py:109 writes the shared model to a fixed `<path>.tmp` (only one topics_model job runs, so harmless in this DAG).
- 2026-09-19 23:37  Resume after Fix #1: same `core_v4_base --keep-going` command (no --forcerun); only minorities s5of8, success, assemble_projects, report, base reran (5 jobs). Verified 5/5, exit 0.
- 2026-09-20 00:20  Fix #2 (in progress): NLLB works shards OOM-killed (64 GB cgroup) because open_staging() sets no DuckDB memory_limit (buffer pool ~80% of node RAM). Infra part: enrichment.smk core_v4_nllb mem_mb 64000 -> 96000 (done). Code part: cap in open_staging (agent, patch fixes/02-enrichment-duckdb-memory-cap.patch).
- 2026-09-20 00:27  Fix #2 done: open_staging() caps DuckDB memory_limit at 40% of the SLURM allocation (SLURM_MEM_PER_NODE / per-CPU / cgroup; none known -> unchanged); regions.py and geolocation.py now use open_staging instead of duckdb.connect on the staging. Files: text_sources.py, regions.py, geolocation.py, test_text_sources.py (+5 tests); enrichment.smk core_v4_nllb mem_mb 64000 -> 96000. Patch: fixes/02-enrichment-duckdb-memory-cap.patch (smk change is not in the patch, it is in the working tree). Tests: 38 passed (text_sources, regions, side_outputs; agent: 102 passed in enrichment/). Resumed the same core_v4_works --keep-going.
- 2026-09-20 00:50  NLLB mem 128000 (2 raises); root cause of the 00:24 OOM not fully explained, see STATUS.
- 2026-09-20 01:22  Infra fix #3: core_v4_pillars work-t0 shards 0,3,4,6 (of 8) OOM-killed at mem_mb=32000 (oom_kill events, empty logs; other shards still running). Raised core_v4_pillars mem_mb 32000 -> 96000 in enrichment.smk (takes effect on the resume run). Also watching topics/minorities/theme (64 GB) and dch (128 GB) for the same.
- 2026-09-20 01:36  Infra fix #4: core_v4_minorities work-t0 shards 0-4,7 OOM-killed at mem_mb=64000 (16 workers). Raised to 128000. Pillars OOM'd at 32 GB on 6 of 8 shards; diagnostic (job 8980866, shard 0/8 tier 0, 128 GB, 4 cpus): 624,673 rows in ~90 s, RSS plateau ~50 GB = DuckDB cap (40% of 128 GB) + python, i.e. RSS tracks the cap; pillars now 96 GB (cap ~38 GB). Wrote real shard-0 output for pillars work-t0 (harmless: the shard job resets its own shard).
- 2026-09-20 16:12  Infra fix #5: tier-1 (work-t1) text enrichments read ALL NLLB translations (nllb CTE in text_sql) and need far more memory than tier 0: core_v4_dch work-t1 shards 3,4,5 OOM-killed at 128 GB within ~1 min. Diagnostic (sbatch 8981025, DCH t1 shard 3/8, a100_80gb, 240 GB): RSS ~91 GB (DuckDB cap 96 GB = 40%), then ~830 rows/s (~1.9 h/shard for 5.6M rows), no OOM. Changed enrichment.smk: topics, theme, minorities, pillars, dch use mem_mb = 240000 for unit work-t1 (tier 0 values unchanged: topics/theme 64 GB, minorities 128 GB, pillars 96 GB, dch 128 GB). Takes effect on the next Snakemake launch; the live Snakemake keeps its old (small) tier-1 resources, so its tier-1 topics/minorities/pillars shards will probably OOM and be rerun after the relaunch. Suspected mechanism (not proven): DuckDB spilling to <db>.tmp on /work leaves dirty page cache that counts against the cgroup.
